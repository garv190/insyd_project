/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.linkedin.com','images.unsplash.com', 'res.cloudinary.com','assets.aceternity.com', 'lh3.googleusercontent.com']
    }
};

export default nextConfig;
